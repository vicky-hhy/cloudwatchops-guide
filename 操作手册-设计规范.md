# cloudwatch-ops 操作手册设计规范

> **版本** v1.0 · **生效日期** 2026-05-11 · **适用对象** 产品经理、技术写作、运营
> 本规范定义 cloudwatch-ops 所有 HTML 操作手册的统一标准。新模块手册必须遵循本规范。

---

## 一、设计原则

### 1.1 风格定位

**macOS / Apple HIG 简约风** —— 浅色背景、克制层级、零装饰冗余,适当点缀 emoji 增加亲和力。

### 1.2 核心准则

| 准则 | 说明 |
|------|------|
| **内容优先** | 视觉为内容服务,不喧宾夺主 |
| **层级清晰** | 标题 / 副标题 / 正文 / 注释,4 级以内 |
| **色彩克制** | 主色仅 1 个(系统蓝),情境色仅 4 种且低饱和 |
| **空白即设计** | 大量留白,呼吸感优先于信息密度 |
| **可读可打印** | HTML 与 PDF 双形态友好 |

### 1.3 受众假设

- **主要读者**: 产品经理、运营、新员工
- **阅读场景**: 桌面浏览器为主,可能打印成 PDF 分发
- **技术水平**: 不假设懂代码,术语必须配解释

---

## 二、信息架构

### 2.1 标准章节顺序

每份手册必须包含以下章节,顺序固定:

```
1. 顶部信息栏(brand + 版本号)
2. Hero 区
   ├─ 面包屑(模块路径)
   ├─ 主标题(emoji + 模块名)
   ├─ 更新日期
   └─ 概述卡片(米黄背景)
3. 主体区(双栏)
   ├─ 左侧:步骤导航(粘性侧边栏)
   └─ 右侧:步骤主体
4. 完成卡片(绿色)
5. FAQ(常见问题)
6. 页脚
```

### 2.2 步骤结构

每个步骤必须包含:

| 元素 | 必选 | 说明 |
|------|------|------|
| 步骤编号 | ✅ | 格式 `步骤 01` / `步骤 08 · 可选` |
| Emoji + 标题 | ✅ | 表意 emoji,标题简短(不超过 12 字) |
| 操作描述 | ✅ | 1-3 句话,关键操作用 `<strong>` 标蓝 |
| 截图 | ✅ | 圆角 + 浅阴影 + 点击放大 |
| 卡片(可选) | 按需 | 注意 / 建议 / 提示 / 警示 |
| 字段表(可选) | 表单类步骤 | 字段 / 说明 / 示例 三列 |
| 有序列表(可选) | 多动作步骤 | 分解为 N 个子动作 |

### 2.3 步骤数量

- **最少**: 3 个步骤(否则不算手册)
- **理想**: 5-10 个步骤(单页可读完)
- **最多**: 15 个步骤(超过应拆分为多份手册)

---

## 三、视觉规范

### 3.1 配色系统

#### 基础色

| 用途 | 色值 | 说明 |
|------|------|------|
| 主背景 | `#fbfbfd` | 极浅灰,非纯白,减少眩光 |
| 卡片背景 | `#fff` | 纯白,与主背景形成微差 |
| 主文字 | `#1d1d1f` | Apple 标准深色 |
| 次要文字 | `#515154` | 描述文字 |
| 辅助文字 | `#86868b` | 元信息、标签 |
| 分隔线 | `#d2d2d7` / `#e8e8ed` | 重要 / 次要 |
| 强调主色 | `#0071e3` | Apple 系统蓝,仅用于关键操作 |

#### 情境卡片色(低饱和)

| 类型 | 背景色 | 文字色 | 用途 |
|------|--------|--------|------|
| ⚠️ **注意** | `#fdf2ec` 粉杏 | `#8a4a1f` | 不可逆操作、命名规范 |
| 💡 **建议** | `#ecf6f1` 薄荷青 | `#2c5f47` | 最佳实践、优化提示 |
| ℹ️ **提示** | `#eef2f9` 雾霾蓝 | `#3a4a6b` | 中性补充、信息说明 |
| 🚨 **警示** | `#fbebec` 淡珊瑚 | `#8a3a3f` | 高危操作、数据风险 |
| 📖 **概述** | `#fffaeb` 米黄 | `#5b4818` | Hero 区下方,模块介绍 |
| 🎉 **完成** | `#f0faf4 → #e8f5ed` 渐变 | `#1d6b3a` | 全部步骤完成后 |

**严禁**:
- 紫色渐变(消费 SaaS 廉价感)
- 高饱和度纯色(刺眼)
- 多种主色混用

### 3.2 字体规范

```css
font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
             "PingFang SC", "Helvetica Neue", sans-serif;
```

| 元素 | 字号 | 字重 | 行高 |
|------|------|------|------|
| H1 大标题 | 44px | 600 | 1.1 |
| H2 步骤标题 | 28px | 600 | 1.2 |
| H2 FAQ 标题 | 28px | 600 | 1.2 |
| 正文 | 17px | 400 | 1.65 |
| 副描述 | 19px | 400 | 1.5 |
| 卡片文字 | 15-16px | 400 | 1.65 |
| 元信息 | 13-14px | 400-500 | 1.4 |
| 代码 / 标签 | 14-15px | 400 | - |

代码字体: `"SF Mono", ui-monospace, Menlo, Monaco, monospace`

### 3.3 圆角规范

| 元素 | 圆角 |
|------|------|
| 完成卡片 | 18px |
| 概述卡片、字段表 | 14px / 12px |
| 普通卡片(注意/建议) | 12px |
| 截图 | 12px |
| 代码标签 | 4px |
| 侧边栏链接 | 8px |

### 3.4 间距规范

| 场景 | 间距 |
|------|------|
| 步骤之间 | 72px |
| 步骤内部(描述↔截图↔卡片) | 20-24px |
| Hero 区上下 padding | 80px / 56px |
| 容器最大宽度 | 1080px |
| 侧边栏宽度 | 240px |
| 左右间距(gap) | 64px |

---

## 四、Emoji 使用规范

### 4.1 选用原则

- **必须表意**: emoji 必须和动作语义对应,不是装饰
- **每步一个**: 步骤标题最多一个 emoji,不可堆砌
- **保持成熟**: 不用过度卡通(🐶🦄🌈),保持职场调性
- **统一基调**: 全文 emoji 风格一致,避免混用 emoji / icon

### 4.2 标准 Emoji 库

#### 步骤类(按动作语义匹配)

| Emoji | 含义 | 适用场景 |
|-------|------|---------|
| 🚪 | 入口 | 进入页面、打开模块 |
| 🔍 | 查找 | 搜索、筛选、定位 |
| ✏️ | 编辑 | 填写表单、输入信息 |
| ☑️ | 勾选 | 多选、复选框操作 |
| 🏷️ | 标签 | 打标签、分类、绑定 |
| ⚙️ | 配置 | 系统设置、参数配置 |
| 🎯 | 验证 | 验证结果、命中目标 |
| 🔀 | 排序 | 拖拽、调整顺序 |
| 📤 | 导出 | 导出数据、下载 |
| 📥 | 导入 | 上传、导入数据 |
| 🗑️ | 删除 | 删除操作 |
| 🔐 | 权限 | 权限配置、密钥管理 |
| 📊 | 数据 | 查看数据、统计 |
| 🔔 | 通知 | 告警、订阅 |
| ✅ | 完成 | 确认成功、提交完成 |
| 🔄 | 同步 | 刷新、重新加载 |

#### 模块类(用于 H1 主标题)

| Emoji | 含义 | 适用模块 |
|-------|------|---------|
| 🗂️ | 分组 | 指标分组、资源分组 |
| 📈 | 监控 | 监控配置、指标管理 |
| 👥 | 用户 | 用户管理、角色 |
| 🔧 | 配置 | 产品配置、参数设置 |
| 💼 | 项目 | 项目管理 |
| 🌐 | 网络 | 网络配置、域名 |
| 📦 | 资源 | 资源管理、容器 |
| 🎨 | 界面 | UI 配置、模板 |

#### 提示类(固定使用)

| Emoji | 卡片类型 | 不可替换 |
|-------|---------|---------|
| ⚠️ | 注意 | ✅ |
| 💡 | 建议 | ✅ |
| ℹ️ | 提示 | ✅ |
| 🚨 | 警示 | ✅ |
| 📖 | 概述 | ✅ |
| 💬 | FAQ | ✅ |
| 🎉 | 完成 | ✅ |

### 4.3 禁用清单

❌ 以下 emoji **不允许**出现在手册中:

- 表情类: 😀😅😂🤣😊(没必要)
- 装饰类: ✨🌟💫🔥💥(廉价感)
- 食物 / 动物类: 🍕🐶🌈(不严肃)
- 重复使用: 同一份手册同一个 emoji 不超过 2 次

---

## 五、HTML 标准模板

### 5.1 完整模板

模板文件位置:`D:\projects\cloudwatchops-guide\操作手册\_template.html`(下方提供)

### 5.2 关键结构片段

#### Hero 区
```html
<section class="hero-wrap">
  <div class="hero-eyebrow">{父模块} / {子模块}</div>
  <h1><span class="emoji">{模块emoji}</span>{模块名}</h1>
  <div class="hero-date">更新日期 · YYYY 年 M 月 D 日</div>
  <div class="summary">
    <p>
      <span class="emoji">📖</span><strong>本手册将指导您完成:</strong>{一句话说明}。
    </p>
  </div>
</section>
```

#### 步骤
```html
<section class="step" id="step-N">
  <div class="step-num">步骤 0N</div>
  <h2 class="step-title"><span class="emoji">{动作emoji}</span>{操作标题}</h2>
  <p class="step-desc">
    {操作描述},关键按钮用 <strong>按钮名</strong>,代码用 <code>code</code>。
  </p>
  <div class="screenshot">
    <img src="../操作手册截图/{模块名}/步骤N_HH-MM-SS.png" alt="{描述}" loading="lazy">
  </div>
  <!-- 可选卡片 -->
  <div class="card {warn|tip|info|alert}">
    <div class="card-emoji">{对应emoji}</div>
    <div class="card-body">
      <span class="card-label">{注意|建议|提示|警示}</span>{卡片内容}
    </div>
  </div>
</section>
```

#### 字段表(用于表单类步骤)
```html
<table class="field-table">
  <thead>
    <tr><th>字段</th><th>说明</th><th>示例</th></tr>
  </thead>
  <tbody>
    <tr><td>{字段名}</td><td>{字段说明}</td><td><code>{示例}</code></td></tr>
  </tbody>
</table>
```

---

## 六、内容写作规范

### 6.1 标题写作

| 类型 | 规则 | 示例 |
|------|------|------|
| H1 主标题 | 名词短语,≤ 8 字 | ✅ 自定义指标分组 |
| 步骤标题 | 动宾结构,≤ 12 字 | ✅ 进入指标配置页面 |
| FAQ 问题 | 完整疑问句,≤ 25 字 | ✅ 创建分组标签时提示"已存在"怎么办? |

### 6.2 描述写作

- **主语省略**: "在表单中填写" 而非 "您在表单中填写"
- **第二人称**: 不用 "您" / "你",直接祈使句
- **关键词强调**: 按钮、字段、菜单名用 `<strong>` 标蓝
- **代码标记**: 输入值、参数名、状态值用 `<code>`
- **引号统一**: 中文引号 「」,不用英文双引号

#### 写作示例

✅ **推荐**:
> 切换到「指标管理 → 外部指标」Tab,通过左侧复选框勾选需要分组的指标 ①,然后点击 **更新指标标签** 按钮 ②。

❌ **避免**:
> 您需要先切换到指标管理这个Tab下面的外部指标,然后您要在左边那一列把您想要分组的指标都打上勾(就是图里①那个位置),完成之后您再点击"更新指标标签"这个按钮(②的位置)。

### 6.3 截图标注

- **使用 ShareX 标注**: 红色圆圈 + 数字编号(①②③)
- **编号统一**: 步骤内多个标注用 ①②③,跨步骤不连续编号
- **位置就近**: 标注紧贴目标元素,不遮挡关键内容
- **范围适中**: 截图包含操作元素 + 上下文,留有边距

### 6.4 卡片使用场景

| 卡片类型 | 必须使用的场景 |
|---------|--------------|
| ⚠️ 注意 | 创建后不可修改的字段、命名规范 |
| 💡 建议 | 最佳实践、效率优化 |
| ℹ️ 提示 | 中性补充、非必读但有用 |
| 🚨 警示 | 删除、清空、不可撤销的操作 |

**频率**:
- 每个步骤最多 1 张卡片
- 全文最多 6 张卡片(避免疲劳)

---

## 七、生成新手册的流程

### 7.1 SOP(标准操作流程)

```
1. 录制截图
   ├─ 用 ShareX 录制(参考 ShareX配置指南.md)
   ├─ 保存路径:操作手册截图/{模块名}/
   └─ 文件名:步骤N_时-分-秒.png

2. 命名规范
   ├─ 模块名:中文短名词(2-6字)
   ├─ 示例:自定义指标分组、新指标接入、存量指标编辑

3. 调用 Claude 生成
   ├─ 提供截图路径
   ├─ 说明目标读者(产品经理 / 运营 / 运维)
   └─ Claude 按本规范生成 HTML

4. 输出位置
   └─ 操作手册/{模块名}.html

5. 校对
   ├─ 浏览器打开预览
   ├─ 核对截图标注 ①②③ 与文字描述一致
   └─ 测试图片放大、侧边栏锚点跳转
```

### 7.2 给 Claude 的标准 Prompt

```
我录了「{模块名}」的操作截图,
位置:D:\projects\cloudwatchops-guide\操作手册截图\{模块名}\
目标读者:{产品经理 / 运营 / 运维}
目的:{一句话说明这个功能解决什么问题}

请按照《操作手册-设计规范.md》生成 HTML 手册,
输出到 D:\projects\cloudwatchops-guide\操作手册\{模块名}.html
```

---

## 八、文件组织规范

### 8.1 目录结构

```
D:\projects\cloudwatchops-guide\
├── 操作手册-设计规范.md          ← 本文件
├── ShareX配置指南.md              ← 截图工具配置
├── 操作手册截图\                  ← 截图根目录
│   ├── 自定义指标分组\
│   ├── 新指标接入\
│   └── ...
└── 操作手册\                      ← HTML 手册输出目录
    ├── _template.html             ← 标准模板(不要直接编辑)
    ├── 自定义指标分组.html         ← 参考样板
    ├── 新指标接入.html
    └── ...
```

### 8.2 命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 截图文件夹 | 中文模块名 | `自定义指标分组/` |
| 截图文件 | `步骤N_HH-MM-SS.png` | `步骤1_20-47-29.png` |
| HTML 文件 | 中文模块名.html | `自定义指标分组.html` |
| 模块标识 | 中文短词 | 不用拼音、不用英文 |

### 8.3 版本管理

- 手册顶部右上角显示版本号 `v1.0` / `v1.1`
- 每次重大更新升级版本号,更新"更新日期"
- 建议使用 Git 管理(可选)

---

## 九、质量检查清单

发布前对照以下清单逐项检查:

### 内容
- [ ] H1 标题简短(≤ 8 字)且包含 emoji
- [ ] 概述卡片 1 句话说清模块目的
- [ ] 每个步骤有截图,且截图含 ①②③ 标注
- [ ] 关键按钮 / 字段名用 `<strong>` 标蓝
- [ ] 中文引号统一用「」
- [ ] FAQ 至少 3 条,覆盖常见错误场景

### 视觉
- [ ] 配色严格遵循规范(主色蓝 + 4 种情境色)
- [ ] 字体使用 Apple 系统字体栈
- [ ] 圆角、间距符合规范
- [ ] Emoji 仅在标题、卡片、完成区使用,正文无 emoji

### 交互
- [ ] 左侧导航点击可跳转
- [ ] 滚动时当前步骤在侧边栏高亮
- [ ] 截图点击可全屏放大
- [ ] 按 Esc 或点击遮罩可关闭放大
- [ ] FAQ 可展开收起
- [ ] 移动端隐藏侧边栏

### 兼容
- [ ] Chrome / Edge / Safari 显示正常
- [ ] 打印预览(Ctrl+P)排版正常
- [ ] 图片相对路径正确(`../操作手册截图/...`)

---

## 十、常见反模式(Anti-patterns)

❌ 这些错误**不要**犯:

1. **滥用 emoji**: 一段文字塞 3 个 emoji,变成微信聊天感
2. **配色失控**: 自己加新颜色卡片,偏离规范色系
3. **步骤过长**: 一个步骤写 5 句话,应拆分
4. **截图缺标注**: 描述里提到"点击 ①"但图里没有红圈
5. **重复信息**: Hero 描述和概述卡片说同样的话
6. **过度技术化**: 用"调用 API"代替"点击按钮",对产品经理不友好
7. **截图不一致**: 同一份手册混用浅色 / 深色模式截图
8. **打破布局**: 自定义内联样式,破坏整体一致性

---

## 附录 A:参考样板

**标杆案例**: `操作手册/自定义指标分组.html`

新手册以此为蓝本,**直接复制后替换内容**,不要从零编写。

## 附录 B:更新日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-05-11 | 初始版本发布,确立 macOS 简约风格 + 低饱和情境色 + emoji 规范 |

---

**规范维护**: 本文档为活文档,新场景出现可补充,但**禁止删减已定义的标准**。
变更需经过评审,确保所有手册一致性。
