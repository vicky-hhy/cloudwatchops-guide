# 操作手册制作完整工作流

## 🎯 目标

用 **ShareX(免费截图) + Claude(AI 整理)** 替代 Scribe/Tango 付费方案,零成本生成专业操作手册。

---

## 📦 准备工作(首次使用,10分钟)

### 1. 安装 ShareX

- 下载地址: https://getsharex.com/
- 安装后按照 `ShareX配置指南.md` 完成配置
- 核心配置:截图保存路径、文件命名、快捷键

### 2. 创建截图存放目录

```
D:\操作手册截图\
  ├── 2026-05-11_用户管理\
  ├── 2026-05-11_权限配置\
  └── 2026-05-12_数据导出\
```

**说明**: ShareX 会根据日期和项目名自动创建子文件夹

---

## 🎬 录制流程(每次制作手册)

### 阶段一:准备阶段(2分钟)

1. **明确手册目标**
   - 这个手册是给谁看的?(新员工 / 客户 / 运维)
   - 要完成什么操作?(创建用户 / 导出报表 / 配置权限)
   - 预计有多少步骤?(建议单个手册不超过 15 步)

2. **准备测试环境**
   - 打开内部平台,登录测试账号
   - 准备好测试数据(如需要填写的表单信息)
   - 清空浏览器缓存/Cookie(避免截图中出现历史数据)

3. **启动 ShareX**
   - 确认托盘图标显示"就绪"
   - 首次截图时会提示输入项目名称,如"用户管理"

---

### 阶段二:录制阶段(5-15分钟)

**核心原则**: 先操作,后截图

#### 标准录制节奏

```
1. 执行一个完整操作(如"点击新增按钮")
   ↓
2. 立即按 Ctrl+Shift+1 截图
   ↓
3. 拖选需要的区域(建议包含操作区域 + 周边上下文)
   ↓
4. 松手自动保存,继续下一步
```

#### 截图范围建议

| 操作类型 | 截图范围 | 示例 |
|---------|---------|------|
| 点击按钮/链接 | 按钮 + 周边菜单/面包屑 | 包含"用户管理 > 新增用户"导航 |
| 填写表单 | 整个表单 + 标题 | 包含"新增用户"对话框标题 |
| 选择下拉菜单 | 下拉框 + 展开的选项 | 包含"部门"字段和所有选项 |
| 查看结果 | 结果区域 + 成功提示 | 包含"创建成功"提示和新增的记录 |
| 错误提示 | 错误信息 + 触发位置 | 包含红色错误文字和对应字段 |

#### 常见场景处理

**场景1: 需要填写很多字段**
- 不要每个字段截一张图,按"逻辑分组"截图
- 示例:基本信息(姓名/工号/部门)截一张,权限配置(角色/权限组)截一张

**场景2: 需要等待加载**
- 等加载完成后再截图,避免截到"加载中"状态
- 如果加载时间很长(>5秒),可以单独截一张"加载中"的图说明

**场景3: 需要展示鼠标悬停效果**
- ShareX 不支持录制鼠标,改用文字说明"将鼠标悬停在 XX 上"
- 或者用 Snipaste(F1 截图后 F3 贴图,可以手动画箭头)

**场景4: 操作失败/报错**
- 故意触发一次错误(如不填必填项直接提交)
- 截图错误提示,在手册中作为"常见问题"

---

### 阶段三:检查阶段(2分钟)

录制完成后,打开截图文件夹检查:

- [ ] 截图数量是否完整(对应操作步骤数)
- [ ] 截图顺序是否正确(按文件名排序)
- [ ] 截图清晰度是否足够(文字可读)
- [ ] 是否包含敏感信息(真实姓名/手机号/内部IP)

**如有问题**: 删除对应截图,重新操作并截图(ShareX 会自动跳过已有编号)

---

## 🤖 Claude 整理阶段(5分钟)

### 方式一:直接拖文件夹(推荐)

1. 在 Claude Code 对话框中输入:

```
我刚录制了"用户管理-新增用户"的操作截图,
请帮我生成一份操作手册,参考 操作手册模板.md 的格式。

目标用户:新入职的运营人员
操作目的:创建新用户账号并分配基础权限
```

2. 把截图文件夹路径发给 Claude:

```
D:\操作手册截图\2026-05-11_用户管理\
```

3. Claude 会:
   - 按顺序读取所有截图
   - 识别每张图的 UI 元素和操作内容
   - 生成结构化的 Markdown 手册
   - 自动填充步骤说明、预期结果、注意事项

---

### 方式二:逐张上传(截图少于5张时)

直接在对话框中拖入截图文件,然后说:

```
这是"导出报表"功能的3个步骤截图,
帮我生成简短的操作说明,每个步骤2-3句话即可。
```

---

### Claude 输出的内容

Claude 会生成一个完整的 Markdown 文件,包含:

- ✅ 目录和功能概述
- ✅ 前置条件检查清单
- ✅ 分步骤操作说明(含截图引用)
- ✅ 每步的预期结果
- ✅ 常见问题 FAQ
- ✅ 注意事项和安全提醒

---

## 📄 导出和分发(2分钟)

### 导出为 PDF(免费)

**方法1: VS Code 预览 + 打印**
1. 在 VS Code 中打开生成的 `.md` 文件
2. 按 `Ctrl+Shift+V` 预览
3. 右键预览窗口 → 打印 → 另存为 PDF

**方法2: Typora(推荐,更美观)**
1. 用 Typora 打开 `.md` 文件
2. 文件 → 导出 → PDF
3. 可选主题(GitHub / Newsprint / Academic)

**方法3: Pandoc(命令行)**
```bash
pandoc 操作手册.md -o 操作手册.pdf --pdf-engine=xelatex
```

---

### 导出为 HTML(可交互)

让 Claude 把 Markdown 转成 HTML:

```
把这份手册转成单文件 HTML,要求:
- 内嵌所有截图(Base64)
- 左侧目录导航,点击可跳转
- 适配移动端
- 可以直接双击打开,不依赖服务器
```

Claude 会生成一个 `.html` 文件,可以:
- 发邮件附件
- 放到内部 Wiki(如语雀/Confluence)
- 部署到内网服务器

---

### 导出为 Word(可编辑)

**方法1: Pandoc**
```bash
pandoc 操作手册.md -o 操作手册.docx
```

**方法2: 在线工具**
- https://www.markdowntoword.com/
- 上传 `.md` 文件,下载 `.docx`

---

## 🔄 迭代和维护

### 手册需要更新时

1. **小改动**(如改个字段名):
   - 直接编辑 Markdown 文件
   - 或让 Claude 修改:"把步骤3中的'用户名'改成'登录账号'"

2. **大改动**(如流程变了):
   - 重新录制变化的步骤
   - 把新截图发给 Claude:"步骤2-4有变化,帮我更新这部分"

3. **版本管理**:
   - 文件名加版本号:`用户管理操作手册_v1.2.md`
   - 或用 Git 管理(推荐)

---

## 📊 效率对比

| 方案 | 录制时间 | 整理时间 | 导出成本 | 总耗时 |
|------|---------|---------|---------|--------|
| **Scribe Pro** | 5分钟 | 自动 | $20/月 | 5分钟 |
| **Tango Pro** | 5分钟 | 自动 | $20/月 | 5分钟 |
| **ShareX + Claude** | 10分钟 | 5分钟 | 免费 | 15分钟 |
| **纯手工(Word)** | 20分钟 | 30分钟 | 免费 | 50分钟 |

**结论**: ShareX + Claude 比纯手工快 70%,比付费工具慢 10 分钟,但完全免费且数据可控。

---

## 💡 进阶技巧

### 1. 批量制作手册

如果要做 10+ 份手册:

```
1. 一次性录制所有模块的截图(按模块分文件夹)
2. 写一个批处理脚本,让 Claude 批量生成
3. 用 Pandoc 批量转 PDF
```

示例 PowerShell 脚本:
```powershell
$folders = Get-ChildItem "D:\操作手册截图\" -Directory
foreach ($folder in $folders) {
    Write-Host "处理: $($folder.Name)"
    # 这里调用 Claude API 或手动处理
}
```

---

### 2. 制作视频版手册

ShareX 也支持录屏:

1. ShareX → 捕获 → 屏幕录制
2. 录制完成后自动保存为 `.mp4`
3. 用剪映/PR 加字幕和标注
4. 上传到内部视频平台

---

### 3. 制作交互式 Demo

如果想要"可点击的演示",不是静态手册:

1. 用 `pm-url2proto` skill 克隆你的平台页面
2. 让 Claude 改成"演示模式"(点击按钮显示提示,不真实提交)
3. 部署到内网,新员工可以"模拟操作"

---

## ❓ 常见问题

### Q: ShareX 截图后文件在哪?

A: 默认在 `C:\Users\你的用户名\Documents\ShareX\Screenshots\`,建议改到 D 盘(见配置指南)。

### Q: Claude 识别截图不准确怎么办?

A: 
- 确保截图清晰,分辨率至少 1080p
- 关键文字用红框标注(ShareX 自带标注工具)
- 手动补充说明:"这张图是点击'新增'按钮后的表单"

### Q: 手册中的截图路径失效?

A: 
- 方法1:把截图和 `.md` 文件放同一目录,用相对路径 `![](./步骤1.png)`
- 方法2:让 Claude 生成 HTML 时内嵌图片(Base64)

### Q: 能自动给截图加序号和箭头吗?

A: 
- ShareX 可以加序号水印(见配置指南"高级技巧")
- 箭头需要手动标注,或者让 Claude 在 HTML 版本中用 CSS 画箭头

---

## 📞 获取帮助

**ShareX 相关**:
- 官方文档: https://getsharex.com/docs/
- 中文教程: 搜索"ShareX 使用教程"

**Claude 相关**:
- 直接在对话中问:"我想实现 XX 效果,手册应该怎么调整?"
- 参考 `操作手册模板.md` 中的格式

**Markdown 转换**:
- Pandoc 文档: https://pandoc.org/
- Typora 官网: https://typora.io/

---

**最后更新**: 2026-05-11  
**维护者**: Claude + 你的团队
