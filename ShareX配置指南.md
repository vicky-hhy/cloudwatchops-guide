# ShareX 配置指南 - 用于操作手册截图

## 一、下载安装

1. 访问官网: https://getsharex.com/
2. 下载安装(免费开源,无广告)
3. 首次启动会弹出快捷键设置,先跳过

---

## 二、核心配置(5 分钟搞定)

### 1. 设置截图保存路径

**路径**: `任务设置 → 截图 → 保存位置`

```
推荐设置:
D:\操作手册截图\%y-%mo-%d_%pn\
```

**说明**:
- `%y-%mo-%d` = 年-月-日(如 2026-05-11)
- `%pn` = 项目名称(首次截图时会提示输入,如"用户管理模块")
- 每次做新手册时会自动创建新文件夹

### 2. 设置文件命名规则

**路径**: `任务设置 → 截图 → 文件命名`

```
推荐格式:
步骤%i_%h-%mi-%s
```

**效果示例**:
```
步骤1_14-23-05.png
步骤2_14-23-12.png
步骤3_14-23-18.png
```

**说明**:
- `%i` = 自动递增编号(每次启动 ShareX 重置为 1)
- `%h-%mi-%s` = 时-分-秒(防止重名)

### 3. 设置快捷键

**路径**: `热键设置`

| 功能 | 推荐快捷键 | 说明 |
|------|-----------|------|
| 区域截图 | `Ctrl + Shift + 1` | 最常用,拖选区域 |
| 窗口截图 | `Ctrl + Shift + 2` | 自动识别窗口边界 |
| 全屏截图 | `Ctrl + Shift + 3` | 整个屏幕 |

**取消上传**: `任务设置 → 目标 → 取消勾选所有上传选项`(只保存本地)

### 4. 截图后操作

**路径**: `任务设置 → 截图后 → 执行的操作`

```
推荐勾选:
☑ 保存图像到文件
☐ 复制图像到剪贴板(可选)
☐ 打开图像编辑器(需要标注时勾选)
☑ 在资源管理器中显示文件(方便确认)
```

---

## 三、使用流程

### 开始录制前

1. 打开 ShareX
2. 确认右下角托盘图标显示"就绪"
3. 打开你的内部平台,准备操作

### 录制步骤

1. 执行一个操作(如"点击新增按钮")
2. 立即按 `Ctrl + Shift + 1` 截图
3. 拖选需要的区域(建议包含完整的操作区域 + 上下文)
4. 松手后自动保存,继续下一步操作
5. 重复 1-4,直到流程结束

### 录制完成后

1. 打开保存文件夹(如 `D:\操作手册截图\2026-05-11_用户管理\`)
2. 检查截图顺序和清晰度
3. 把整个文件夹路径告诉 Claude

---

## 四、高级技巧(可选)

### 自动添加序号水印

**路径**: `图像 → 图像效果`

添加效果:
- `绘图 → 文本(水印)` → 内容设置为 `%i`
- 位置:左上角
- 字体:微软雅黑 Bold 48px
- 颜色:红色 + 白色描边

**效果**: 截图左上角自动显示"1"、"2"、"3"...

### 自动压缩图片

**路径**: `任务设置 → 高级 → 图像质量`

```
格式: PNG(清晰) 或 JPG(文件小)
JPG 质量: 85(平衡清晰度和大小)
```

---

## 五、常见问题

**Q: 截图编号不连续怎么办?**  
A: ShareX 重启后编号会重置。建议一次性录完一个模块的所有步骤。

**Q: 截图太大,文件夹几百 MB?**  
A: 改用 JPG 格式,质量设 80-85,单张图从 2MB 降到 200KB。

**Q: 需要标注箭头/文字怎么办?**  
A: 勾选"截图后 → 打开图像编辑器",ShareX 自带简单标注工具。或者用 Snipaste(F1 截图 → F3 贴图 → 右键标注)。

**Q: 能录制鼠标点击动作吗?**  
A: ShareX 不支持。需要录制鼠标轨迹用 ScreenToGif(录 GIF)或 OBS(录视频)。

---

## 六、配置文件备份(可选)

配置完成后,备份设置:

**路径**: `应用程序设置 → 导出/导入 → 导出设置`

保存为 `ShareX-操作手册配置.sxcu`,下次重装直接导入。
